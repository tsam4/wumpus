// REMOVED: this test targeted datasets 4 and 5 (occupied-cell prior feature)
// which are not part of the DE424 mini-project scope (datasets 1-3 only).
// The function signatures it relied on (build_transition_adj with occ/bool args,
// compute_emission_log with occ/bool args) do not exist in wumpus_model.hpp.
//
// File kept as a placeholder to avoid CMakeLists.txt breakage.
// If dataset 4/5 support is added in future, restore from git history.

int main() { return 0; }
